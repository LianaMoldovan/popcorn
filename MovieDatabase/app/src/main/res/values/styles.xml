<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@android:transition/slide_right</item>
        <item name="android:windowExitTransition">@android:transition/slide_left</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>

        <!-- global custom font style-->
        <item name="android:textViewStyle">@style/LatoTextViewtyle</item>
    </style>

    <style name="ToolbarTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/blue</item>
    </style>

    <!--custom font style-->
    <style name="LatoTextViewtyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/lato_font</item>
        <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
        <item name="fontFamily">@font/lato_font</item>
    </style>
</resources>
